# USER CONTROLLER
userModel-controller.create-userModel-service.userModel-not-null = "request must not be null"
userModel-controller.get-userModel-data-service.username-not-blank = "username must not be null nor empty"

# AUTHENTICATION CONTROLLER
authentication-controller.authenticate-service.credentials-not-blank = "credentials must not be null nor empty"

# NAVIGATION CONTROLLER
navigation-history-controller.navigation-service.navigation-not-null = "request must not be null"